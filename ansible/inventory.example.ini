[speedtest]
speedtest1.example.net ansible_user=ubuntu

# For a multi-PoP deployment, add one host per region and give each a
# different tr143_server_name in group_vars/<region>.yml — see README.md
# "Scaling to multiple regions".
